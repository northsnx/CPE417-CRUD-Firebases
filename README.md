# 📌 CPE417-CRUD-Firebases

“This project, titled CPE417-CRUD-Firebases, was developed for HW12 in the CPE417 course at Sripatum University. It demonstrates my ability to build a CRUD application using Firebase and apply the concepts learned throughout the course.”

---

## 🚀 Features

* Add new data
* Read / fetch data from Firestore
* Update existing data
* Delete data
* Organized structure and reusable Firebase config

---

## 🔥 Setup Firebases

### 1. Create Firebase Project

1. Go to the Firebase Console.
2. Click **Create Project** and enter a project name.
3. Add a **Web App (</>)** to obtain your Firebase configuration.
4. Navigate to **Build → Firestore Database → Create Database**.
5. Select **Test Mode** (or configure Rules as needed).

### 2. Firebase Config
1. Past you apikey and Auth to use this
2. Example Auth key
```bash
const firebaseConfig = {
  apiKey: "your-api-key",
  authDomain: "your-auth-domain",
  projectId: "your-project-id",
  storageBucket: "your-storage-bucket",
  messagingSenderId: "your-messaging-sender-id",
  appId: "your-app-id",
  measurementId: "your-measurement-id"
};
```

---
